# autoconf-generated (overwrites the committed Makefile when present)
Makefile

# Make build tree
build/

# C build artifacts. bin/secret is now a compiled binary (built from
# lib/ + src/), not a tracked script — build it with `make`.
*.o
*.a
*.so
/bin/secret
